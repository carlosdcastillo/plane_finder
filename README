plane_finder.py program 
by Carlos D. Castillo

Receives two point clouds an input and an output. Finds the largest upright
plane and cleans it up of other junk.

Usage:
python plane_finder.py input.pcd output.pcd


